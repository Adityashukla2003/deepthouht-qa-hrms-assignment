import { test, expect } from '@playwright/test';

test('smoke - app loads and login page is reachable', async ({ page }) => {
  test.skip(true, 'Assignment environment placeholder: smoke coverage example.');
});
